# 將 Google Apps Script 部署為「網路應用程式」後的網址（/exec 結尾）
VITE_APPS_SCRIPT_URL=

# 選用：團購詳情「點餐占卜」功能（OpenAI API；金鑰會出現在打包後的前端，正式環境請改後端代理）
# VITE_OPENAI_API_KEY=
# 選用：占卜模型（預設 gpt-4o-mini-search-preview，具網搜；可改 gpt-5-search-api 等，依帳號可用模型為準）
# VITE_OPENAI_FORTUNE_MODEL=
#
# GitHub Actions 部署（.github/workflows/deploy-pages.yml）請到
#   Repo → Settings → Secrets and variables → Actions
# 新增同名 secret：VITE_APPS_SCRIPT_URL（必填）、VITE_OPENAI_API_KEY（選填，與本機 .env.local 相同用途）

# 選用：部署到 GitHub Pages 專案站時，設為 /你的-repo-名稱/（前後都要有 /）
# 本機開發請勿設定或設為 /
# VITE_BASE_PATH=/office-meal-order/
